services:
  ollama:
    image: ollama/ollama:latest
    container_name: very-small-llm-ollama
    restart: unless-stopped
    ports:
      - "127.0.0.1:11434:11434"
    volumes:
      - ollama-data:/root/.ollama

volumes:
  ollama-data: